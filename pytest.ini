[pytest]
testpaths = tests
addopts = -ra
markers =
    slow: test yang membaca seluruh dataset atau menjalankan alur berat
    streamlit: smoke test UI melalui Streamlit AppTest
    notebook: eksekusi ulang notebook melalui nbclient
